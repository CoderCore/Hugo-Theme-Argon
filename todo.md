# TODO / Goal

本文件只维护当前目标和清单；改动历史记录在 [`update.md`](./update.md)。

## 总体 Goal

维护一个可直接使用的 Argon Hugo 主题，并提供安全、可选的 Cloudflare 阅读量与自建评论后端。

## 已完成

### 主题前端

- [x] 页面迁移：首页、归档、分类、标签、文章、说说、时间线、留言板、作者页、关于页。
- [x] Argon 布局：左栏、右栏、文章目录、单列/三栏、瀑布流和移动端响应式。
- [x] 内容能力：短代码、代码高亮、数学公式、图片处理、搜索、SEO、i18n、分享和懒加载。
- [x] 兼容处理：分页、评论嵌套、暗色模式、代码块背景、页脚文案和跨路径导航。
- [x] 评论前端：Argon 样式、分页、回复、头像、Markdown 安全渲染、GitHub 登录状态和退出登录。

### Worker / D1

- [x] 阅读量 API、站点总量、批量读取和管理员接口。
- [x] 评论 API：列表、回复、分页、头像关联和父评论校验。
- [x] 评论管理：作者编辑、回复树删除策略、紧凑删除占位、权限校验和 Argon 风格行内操作。
- [x] GitHub OAuth：state、PKCE、会话 Cookie、登录态查询和退出登录。
- [x] 安全加固：CSRF、Origin/Fetch Metadata、JSON Content-Type、外部请求超时、响应安全头和限流。
- [x] 本地/生产 CORS 配置分离；生产不允许 localhost，正式站点仍可读取公开接口。

## 当前 Goal

### G1：评论生产化收尾

- [ ] 用户在 GitHub 轮换曾暴露的 Client Secret，并更新 Worker Secret。
- [ ] 完成一次真实 GitHub 登录回调和登录后发表评论验收。
- [ ] 设计审核、举报和管理员操作；作者编辑/回复树删除已完成，待真实 OAuth 验收。
- [ ] 设计垃圾评论、举报和内容保留策略。

### G2：阅读量生产验收

- [ ] 在正式站点产生一次阅读量并核对文章数和站点总量。
- [ ] 验证管理员 `/admin/` 的读取、修改和删除流程。

### G4：后台第一、二步（当前执行范围）

- [x] 将管理入口从 `/settings/` 整理为 `/admin/`，提供 Argon 风格总览、阅读量和评论管理页。
- [x] Worker 增加管理员状态、阅读量和评论的服务端分页接口；管理写操作继续使用 `VIEW_COUNTER_ADMIN_KEY`。
- [x] 阅读量后台展示文章标题、路径、计数和站点总量；评论后台支持按文章、作者、状态筛选。
- [x] 管理数据页限制单页最多 50 条，避免继续调用全量管理接口。
- [ ] 用本地 Worker + 示例站手动验收分页、筛选、编辑、删除和权限边界。

以下内容不在本轮：每日趋势图表、Cloudflare Analytics 接入、操作日志、文章编辑、媒体上传和第四步兼容性清理。

### G3：可选优化

- [ ] 为公开评论列表评估短时缓存或游标分页。
- [ ] 补充 Clamp、旧格式 lazyload、Banner 等正向示例。
- [ ] 站点规模明显增长后再评估 Pagefind 和公网性能测试。

## 原则

- 生产 Origin 只写真实站点，不把 localhost 部署到线上。
- 不把任何 Secret 写入主题、Hugo 配置、前端代码或 Git。
- 评论 Markdown 只做安全渲染，不执行用户 JavaScript 或不可信 HTML。
- 前端完成项不重复重做；只有出现可复现问题时才修改。
- `todo.md` 保持清单，详细过程写入 `update.md`。

## 完成判定

- 前端：示例站和目标站构建通过，核心页面和移动端无已知阻断问题。
- 评论：真实 OAuth 回调、评论发布、会话退出、限流和安全边界验证通过。
- 阅读量：正式站点读取、递增和管理员修正流程验证通过。
- 发布：主题仓库提交完整，博客是否同步由用户单独决定。
